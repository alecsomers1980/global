# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Migrations only (npm run migrate). Supabase dashboard: Connect > Session pooler.
# Carries the database password — never commit a filled-in value.
SUPABASE_DB_URL=

# Site
NEXT_PUBLIC_SITE_URL=http://localhost:3010

# PayFast — these are PayFast's PUBLIC SANDBOX credentials and are correct
# as-is for testing. Replace with the live merchant values at handover and
# set PAYFAST_MODE=production.
PAYFAST_MERCHANT_ID=10000100
PAYFAST_MERCHANT_KEY=46f0cd694581a
PAYFAST_PASSPHRASE=
PAYFAST_MODE=sandbox

# Email
RESEND_API_KEY=
ORDER_NOTIFY_EMAIL=friedsgrobler@gmail.com
