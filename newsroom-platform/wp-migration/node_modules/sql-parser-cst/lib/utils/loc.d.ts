import { Node, Whitespace } from "src/cst/Node";
/** Attaches source location data to node (if includeRange setting enabled) */
export declare const loc: (node: Node | Whitespace) => Node | Whitespace;
