Output ONLY file blocks in EXACTLY this format (no prose, no outer fences):

===FILE: <project-relative-path>===
<full file contents>
===END===

Next.js 14 App Router + TypeScript + Tailwind. Restyle three pages to an elegant, premium "theatre admin" look. Brand: navy #060A3C, royal blue #0F3193, mint #62DAA9 (single action accent), cream #FFFADB, navy-muted #3D4067, canvas #F4F4F1, line #E6E6E1; Montserrat; radius 3px; shadows `shadow-card`/`shadow-lift`; animation `animate-fade-up`. AVAILABLE: `import Logo from "@/components/brand/Logo"` (props {className?, href?}; cream wordmark, use ONLY on navy bg), `import AppHeader from "@/components/ui/AppHeader"` (props {title, subtitle?, staffName?}), lucide-react icons. PRESERVE ALL LOGIC/behaviour exactly — only restyle. No glassmorphism/mesh/neon.

===FILE: app/page.tsx===
A premium landing. Full-height (min-h-screen) deep mv-navy canvas, content centred. Show <Logo className="h-12 sm:h-16 w-auto mx-auto" href={null} />. Below: an elegant cream tagline/headline (font-heading, e.g. "Festival Operations") and a muted cream/70 subline ("Manage performances, complimentary tickets, and more — all in one place."). A single primary CTA: a mint button with navy text linking to /staff-login labelled "Staff sign-in" (use next/link). Add a subtle decorative touch in keeping with the logo's star motif (e.g. a small mint ✦/sparkle or a thin mint divider) — tasteful, not busy. A small cream/50 footer line "Maynardville Open-Air Festival". Apply animate-fade-up to the main block. No data calls.

===FILE: app/staff-login/page.tsx===
Server component with the SAME signature and logic as this current version — KEEP the searchParams handling (sent / error==="expired" / error==="denied"), the `isDev = process.env.NODE_ENV !== "production"` gate, the form `action="/api/auth/request-link" method="post"` with an email input `name="email"` (required), and the two dev-login anchor links to `/api/auth/dev-login?role=Admin&name=Jaco` and `/api/auth/dev-login?role=Box%20Office&name=Jeff`. RESTYLE: full-height split feel on mv-navy; a centred card (bg-mv-navy with a subtle border border-mv-cream/15, shadow-2xl, rounded). Put <Logo className="h-10 w-auto mx-auto mb-6" href="/" /> at the top of the card. Heading "Staff sign-in" (font-heading cream). Status messages: success uses mint/20 bg + cream text; errors use a red-400/20 bg + cream text. Email label cream; input: bg-white/5, border border-mv-cream/20, text cream, placeholder cream/40, focus ring mint. PRIMARY button = mint bg with mv-navy text, font-semibold, full width, hover slightly brighter (the single strong accent). Divider, then the dev block: small uppercase cream/50 label "Dev quick-login" and the two links styled as subtle bordered cream rows (border-mv-cream/20 hover:bg-mv-cream/10). Use font-sans (not "font-body"). Replace the bare `<a>` dev links is fine (they hit API routes).

===FILE: app/dashboard/page.tsx===
The role-aware hub. KEEP this exact logic: `import { getStaffSession } from "@/lib/session";` `export const dynamic = "force-dynamic";` a `navItems` array with these items (title, href, description, roles) — Approvals (/approvals, ["Admin"]), Box Office (/box-office, ["Admin","Box Office"]), Festival Leadership (/leadership, ["Admin"]), PR & Media (/pr-media, ["Admin","PR & Media"]), Sponsorship (/sponsorship, ["Admin","Sponsorships"]), Operations (/operations, ["Admin","Operations"]), Sales vs Comp Report (/reports, ["Admin"]). `const staff = getStaffSession();` if null → a branded prompt (mv-navy full-screen card with <Logo href={null} className="h-10 mx-auto mb-4"/>, a line about signing in, and a mint button link to /staff-login). Else filter navItems by staff.role and render: an <AppHeader title="Staff Hub" staffName={staff.name} /> at the top, then a main on bg-mv-canvas with a max-w-6xl container, a small "Signed in as {name} ({role})" is already in the header so don't repeat it; render the allowed items as a responsive grid (1/2/3 cols) of cards. Each card: a next/link, bg-white, border border-mv-line, rounded, shadow-card, p-6, hover:shadow-lift hover:-translate-y-0.5 transition, with a lucide icon in a mint/15 rounded square at top (pick a fitting icon per item: Approvals→ClipboardCheck, Box Office→Ticket, Festival Leadership→LayoutDashboard, PR & Media→Megaphone, Sponsorship→Handshake, Operations→ClipboardList, Sales vs Comp Report→BarChart3), the title (font-heading text-mv-navy), and the description (text-sm text-mv-navy-muted). Add a small footer note about email magic-link sign-in. Apply animate-fade-up to the grid.

Output each file as its own ===FILE:===/===END=== block, relative paths. No commentary.
