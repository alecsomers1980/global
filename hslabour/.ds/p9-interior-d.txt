You are the implementation engineer. Output ONLY files, in this EXACT format, no prose, no markdown fences around blocks:

===FILE: <project-relative-path>===
<full file content>
===END===

STACK: Next.js 16 App Router, React 19, TS, Tailwind v4. `@/` = root. lucide-react. SERVER component. `Container` default export at `@/components/site/Container`. `PageHeader` at `@/components/site/PageHeader` props `{ eyebrow, title, intro?, imageSrc?, imageAlt?, children? }`.
BRAND: `navy` #011D58, `green` #46D835, `green-dark` #2FA823, `mint` #EDF6E6, muted slate-600.
ESCAPING: `react/no-unescaped-entities` is ERROR — in JSX TEXT use `&apos;` not raw apostrophe; raw `&` in JSX text OK; in attribute/prop strings literal `&`/`'` are fine.

GENERATE EXACTLY THIS ONE FILE.

----------------------------------------------------------------
FILE — app/(marketing)/contact/page.tsx
Themed contact page. SERVER component. Keep LocalBusinessJsonLd, company, cities, ContactForm.
- Imports: `import type { Metadata } from "next";` `import { company } from "@/lib/site/company";` `import { cities } from "@/lib/site/cities";` `import ContactForm from "@/components/forms/ContactForm";` `import LocalBusinessJsonLd from "@/components/seo/LocalBusinessJsonLd";` `import Container from "@/components/site/Container";` `import PageHeader from "@/components/site/PageHeader";` `import { Mail, Phone, Clock } from "lucide-react";`
- Keep metadata EXACTLY:
  export const metadata: Metadata = {
    title: "Contact H&S Labour Brokers",
    description: "Get in touch with H&S Labour Brokers. We provide recruitment, TES, payroll, vetting, HR & IR, and CV response handling services across South Africa.",
  };
- default export `ContactPage`, return a fragment:
  - `<LocalBusinessJsonLd />`
  - `<PageHeader eyebrow="Contact" title="Let's talk" intro="Whether you need permanent staff, temporary workers, or help with payroll and compliance — we're here. Reach out using the form and we'll get back to you within one business day." imageSrc="/images/about-handshake.jpg" imageAlt="Get in touch with H&S Labour Brokers" />` (title/intro are prop strings — literal apostrophes are fine here, do NOT use entities in props).
  - `<section className="bg-white py-20 sm:py-24"><Container><div className="grid gap-12 lg:grid-cols-2">`:
    LEFT `<div>`:
      - h2 `text-2xl font-bold tracking-tight text-navy` "Contact details".
      - `<div className="mt-6 space-y-5">` with THREE rows; each row `flex items-start gap-4`: a tile `<div className="flex h-11 w-11 shrink-0 items-center justify-center rounded-xl bg-mint text-green-dark"><ICON className="h-5 w-5" aria-hidden /></div>` then a `<div>` with `<p className="text-sm font-semibold text-navy">LABEL</p>` and the value:
        1. Mail, LABEL "Email", value `<a href={`mailto:${company.email}`} className="text-slate-600 transition-colors hover:text-green-dark">{company.email}</a>`
        2. Phone, LABEL "Phone", value `<a href="tel:0114684192" className="text-slate-600 transition-colors hover:text-green-dark">011 468 4192</a>`
        3. Clock, LABEL "Office hours", value `<p className="text-slate-600">Mon–Fri, 8:30–16:00</p>`
      - h3 `mt-10 text-lg font-semibold text-navy` "Areas we serve"; `<ul className="mt-4 grid grid-cols-2 gap-2 text-sm text-slate-700">` mapping `cities` → `<li key={city.slug} className="flex items-center gap-2"><span aria-hidden="true" className="text-green-dark">•</span>{city.name}, {city.province}</li>`.
      - `{company.accreditations.length > 0 && (` <div className="mt-10"> h3 `text-lg font-semibold text-navy` "Accreditations" + `<ul className="mt-3 space-y-1 text-sm text-slate-700">` mapping accreditations → `<li key={i}>{acc}</li>` (use index i) </div> `)}`.
    RIGHT `<div className="rounded-2xl border border-slate-200 bg-slate-50 p-8"><ContactForm heading="Send us a message" /></div>`.
    Close all tags.
