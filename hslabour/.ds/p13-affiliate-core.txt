You are the implementation engineer. Output ONLY files, in this EXACT format, no prose, no markdown fences around blocks:

===FILE: <project-relative-path>===
<full file content>
===END===

STACK: Next.js 16 App Router, React 19, strict TypeScript, Tailwind v4. `@/` = project root. `lucide-react` for icons.
BRAND utilities exist: `bg-navy` `text-navy` (#011D58), `bg-green` `text-green` (#46D835), `text-green-dark`/`bg-green-dark` (#2FA823), `bg-mint` (#EDF6E6), `text-ink`. Font Roboto via root layout.
ESCAPING: `react/no-unescaped-entities` is an ERROR — use `&apos;` for apostrophes in JSX TEXT; literal apostrophes fine in attribute/prop strings.

GENERATE EXACTLY THESE THREE FILES.

----------------------------------------------------------------
FILE — lib/affiliate/code.ts
Deterministic tracking-code generator (ported verbatim, typed). Output exactly:

export function generateAffiliateCode(firstName: string | null, userId: string): string {
  const namePart = (firstName || "AFF").toUpperCase().replace(/[^A-Z]/g, "").slice(0, 5);
  const idPart = userId.replace(/-/g, "").slice(-4).toUpperCase();
  return `${namePart}${idPart}`;
}

----------------------------------------------------------------
FILE — app/affiliate/actions.ts
Server action to save payout details (ported verbatim, typed). Output exactly:

"use server";

import { revalidatePath } from "next/cache";
import { createClient } from "@/lib/supabase/server";

export type BankResult = { error?: string; success?: boolean };

export async function saveBankDetails(formData: FormData): Promise<BankResult> {
  const supabase = await createClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();
  if (!user) return { error: "Unauthorized" };

  const bank_name = String(formData.get("bank_name") ?? "").trim();
  const account_number = String(formData.get("account_number") ?? "").trim();
  const branch_code = String(formData.get("branch_code") ?? "").trim();
  if (!bank_name || !account_number || !branch_code) {
    return { error: "All fields are required" };
  }

  const { error } = await supabase
    .from("profiles")
    .update({ bank_name, account_number, branch_code })
    .eq("id", user.id);
  if (error) {
    console.error("Failed to save bank details:", error);
    return { error: "Database error. Please try again." };
  }

  revalidatePath("/affiliate");
  return { success: true };
}

----------------------------------------------------------------
FILE — app/affiliate/BankDetailsForm.tsx
Client component, H&S branded. Stores banking info for payouts.
- `"use client";` imports: `useState` from "react"; `{ saveBankDetails }` from "./actions"; icons `Lock, Loader2, CheckCircle2, AlertCircle` from "lucide-react".
- Props (TS interface): `{ initialBankName?: string | null; initialAccountNumber?: string | null; initialBranchCode?: string | null }`.
- Local state: `status: "idle" | "saving" | "success" | "error"` and `message: string`.
- `async function handleAction(formData: FormData)`: set status "saving", clear message; `const result = await saveBankDetails(formData);` if `result?.error` → status "error", message = result.error; else status "success", message "Bank details securely saved.", and `setTimeout(() => setStatus("idle"), 3000)`.
- Card: `<div className="rounded-2xl border border-slate-200 bg-white p-6">`. Header row: a mint tile `<div className="flex h-10 w-10 items-center justify-center rounded-lg bg-mint text-green-dark"><Lock className="h-5 w-5" /></div>` then `<h2 className="text-lg font-bold text-navy">Payout details</h2>` and `<p className="text-sm text-slate-500">Securely store your banking information for commission payouts.</p>`.
- `<form action={handleAction} className="mt-6 grid gap-4 sm:grid-cols-3">`: three inputs (Bank name → name="bank_name" defaultValue={initialBankName ?? ""} required placeholder="e.g. FNB"; Account number → name="account_number" defaultValue={initialAccountNumber ?? ""} required; Branch code → name="branch_code" defaultValue={initialBranchCode ?? ""} required). Each label `text-sm font-medium text-slate-700`; each input `w-full rounded border border-slate-300 px-3 py-2 text-ink outline-none focus:border-green focus:ring-2 focus:ring-green/40`.
- Submit button (full width on its own row, `sm:col-span-3`): `inline-flex items-center justify-center gap-2 rounded bg-green px-6 py-3 text-sm font-semibold text-navy hover:bg-green-dark disabled:opacity-60`, disabled when status==="saving". When saving show `<Loader2 className="h-4 w-4 animate-spin" /> Saving` else `<Lock className="h-4 w-4" /> Save`.
- Below the form, if `message`: a box `mt-4 flex items-center gap-2 rounded p-3 text-sm` — error variant `border border-red-200 bg-red-50 text-red-800` with `<AlertCircle className="h-4 w-4" />`; success variant `border border-green/30 bg-mint text-navy` with `<CheckCircle2 className="h-4 w-4" />`.
