You are the implementation engineer for a website redesign. Output ONLY files, in this EXACT format, with no prose, no explanations, and no markdown code fences around the blocks:

===FILE: <project-relative-path>===
<full file content>
===END===

PROJECT STACK
- Next.js 16 (App Router), React 19, TypeScript, Tailwind CSS v4. Path alias `@/` = project root. Icons from `lucide-react`. Links via `next/link`.
- All SERVER components — do NOT add "use client".
- Container exists at `@/components/site/Container` (default export OK).

CRITICAL — JSX ESCAPING RULE (the project lints `react/no-unescaped-entities` as an ERROR):
- NEVER put a raw apostrophe `'` in JSX TEXT. Write `&apos;` instead (e.g. `H&amp;S Labour&apos;s`, `you&apos;re`).
- A raw `&` ampersand in JSX text is fine (e.g. `Vetting & Screening`). Apostrophes inside ordinary JS/TS strings (object data) are fine and need no escaping.

BRAND TOKENS (Tailwind utilities from globals.css): `navy` #011D58, `navy-dark` #010F33, `green` #46D835 (vivid — put `text-navy` on green fills, never white), `green-dark` #2FA823, `ink` #0F1E3D. Muted = slate-600 (on white) / slate-300-400 (on navy). Font Roboto globally.

DESIGN LANGUAGE (portergroup.com-inspired, H&S colours): generous whitespace; big bold headings `font-bold/extrabold tracking-tight text-balance`; section padding `py-20 sm:py-28`; cards `rounded-2xl border border-slate-200 bg-white p-8` with hover `hover:-translate-y-1 hover:shadow-xl transition-all duration-300`; eyebrow `text-sm font-semibold uppercase tracking-wider text-green-dark`; primary button `inline-flex items-center justify-center gap-2 rounded-lg bg-green px-7 py-3.5 text-sm font-semibold text-navy shadow-sm transition-all duration-300 hover:bg-green-dark hover:-translate-y-0.5`.

GENERATE EXACTLY THESE THREE FILES.

----------------------------------------------------------------
FILE 1 — components/site/AreasOfFocus.tsx
Porter Group "Areas of Focus" grid, driven by the existing services data. Default export `AreasOfFocus`. SERVER component.
- `import Container from "@/components/site/Container";` `import Link from "next/link";` `import { ArrowRight } from "lucide-react";` `import { services } from "@/lib/site/services";`
- `services` is an array; each item has `slug` (string), `name` (string), and `tagline` (string).
- `<section className="bg-white py-20 sm:py-28">` > `<Container>`.
- Centered header `mx-auto max-w-2xl text-center`: eyebrow "Our Services"; h2 `mt-3 text-3xl font-bold tracking-tight text-navy sm:text-4xl text-balance` "Everything your workforce needs"; subtext `mt-4 text-lg text-slate-600` "From permanent placements to large-scale TES — every engagement backed by deep compliance expertise and our replacement guarantee."
- Grid `mt-14 grid gap-6 sm:gap-8 grid-cols-1 md:grid-cols-2 lg:grid-cols-3`.
- For each service render a `<Link key={service.slug} href={`/services/${service.slug}`} className="group relative flex flex-col rounded-2xl border border-slate-200 bg-white p-8 transition-all duration-300 ease-out hover:-translate-y-1 hover:border-green hover:shadow-xl">`:
  - `h3` `text-lg font-semibold text-navy` = `{service.name}`
  - `p` `mt-2 text-sm leading-relaxed text-slate-600` = `{service.tagline}`
  - span `mt-4 inline-flex items-center gap-1 text-sm font-medium text-green-dark` containing "Learn more" + `<ArrowRight className="h-4 w-4 transition-transform group-hover:translate-x-1" />`.

----------------------------------------------------------------
FILE 2 — components/site/Testimonials.tsx
Client testimonials (Porter Group "what clients say"). Default export `Testimonials`. SERVER component.
- Define a local `const testimonials = [...]` array of two objects with fields `quote`, `name`, `role`. Use EXACTLY this content (these are plain JS strings — apostrophes are fine here):
  1. quote: "Smart, adaptable, and cost-effective. H&S Labour's tailored packages, covering everything from response handling to psychometrics, prove they understand and deliver to unique business needs. A decade of partnership speaks volumes.", name: "Andrew", role: "Director"
  2. quote: "H&S Labour has been a vital partner since 2005. Their tailored solutions, from recruitment to HR services, showcase a deep understanding of our needs. A strategic choice for business growth!", name: "Jolie", role: "HR Manager"
- `<section className="bg-slate-50 py-20 sm:py-28">` > `<Container>`.
- Centered header: eyebrow "Testimonials"; h2 `mt-3 text-3xl font-bold tracking-tight text-navy sm:text-4xl text-balance` "What our clients say".
- Grid `mt-14 grid gap-6 sm:gap-8 md:grid-cols-2`.
- Each testimonial → a card `flex flex-col rounded-2xl border border-slate-200 bg-white p-8 shadow-sm`:
  - a lucide `Quote` icon `h-8 w-8 text-green` at top.
  - blockquote `mt-4 flex-1 text-base leading-relaxed text-slate-700` rendering `{t.quote}` — IMPORTANT: since the quote string contains an apostrophe it is a JS string in braces, which is safe; do not hardcode it as raw JSX text.
  - footer `mt-6`: `div` `font-semibold text-navy` = `{t.name}`, `div` `text-sm text-slate-500` = `{t.role}`.
- Imports: Container from "@/components/site/Container"; `{ Quote }` from lucide-react.

----------------------------------------------------------------
FILE 3 — app/(marketing)/page.tsx
The redesigned homepage composition. SERVER component with a metadata export.
- Imports:
  `import type { Metadata } from "next";`
  `import Link from "next/link";`
  `import Hero from "@/components/site/Hero";`
  `import AudienceSplit from "@/components/site/AudienceSplit";`
  `import AreasOfFocus from "@/components/site/AreasOfFocus";`
  `import Stats from "@/components/site/Stats";`
  `import Testimonials from "@/components/site/Testimonials";`
  `import Container from "@/components/site/Container";`
  `import LocalBusinessJsonLd from "@/components/seo/LocalBusinessJsonLd";`
  `import { ArrowRight } from "lucide-react";`
- Keep this metadata export:
  export const metadata: Metadata = {
    title: "H&S Labour Brokers — Recruitment, TES & Payroll in South Africa",
    description: "Permanent & contract recruitment, TES, payroll, vetting and HR across South Africa. Since 1998 — compliant, guaranteed.",
  };
- Default export `HomePage` returning a fragment `<>...</>` in THIS order:
  1. `<LocalBusinessJsonLd />`
  2. `<Hero />`
  3. `<AudienceSplit />`
  4. `<AreasOfFocus />`
  5. `<Stats />`
  6. ABOUT TEASER section: `<section className="bg-white py-20 sm:py-28">` > `<Container>` > a `grid items-center gap-12 lg:grid-cols-2`:
     - Left column: eyebrow `text-sm font-semibold uppercase tracking-wider text-green-dark` "About Us"; h2 `mt-3 text-3xl font-bold tracking-tight text-navy sm:text-4xl text-balance` "Your trusted workforce partner since 1998"; two paragraphs `mt-6 text-lg text-slate-600` — first: "Established in 1998, H&amp;S Labour Brokers has grown into a diverse portfolio of recruitment, Temporary Employment Services, response handling, vetting, psychometrics and HR services across many industries."; second `mt-4 text-lg text-slate-600`: "Our mission is simple — create strong, lasting partnerships with our clients through effective collaboration." Then a primary green button Link to `/about` `mt-8` text "More About Us" + ArrowRight (`h-4 w-4`).
     - Right column: a navy quote card `rounded-2xl bg-navy p-10 text-white`: a big `text-2xl font-bold` line "We aspire to be a premier service provider — delivering a bespoke, holistic experience to clients and candidates alike." and below `mt-6 text-sm uppercase tracking-wider text-green` "Our Vision".
  7. `<Testimonials />`
  8. FINAL CTA section: `<section className="bg-slate-50 py-20 sm:py-28">` > `<Container>` > centered `mx-auto max-w-2xl text-center`: h2 `text-3xl font-bold tracking-tight text-navy sm:text-4xl text-balance` "Ready to hire or looking for work?"; p `mt-4 text-lg text-slate-600` "Whether you need staff or your next job, H&amp;S Labour Brokers is here to help."; a button row `mt-10 flex flex-col sm:flex-row gap-4 justify-center`: primary green button Link `/employers` "Hire Staff" + ArrowRight; secondary button Link `/jobs` (`inline-flex items-center justify-center gap-2 rounded-lg border border-slate-300 bg-white px-7 py-3.5 text-sm font-semibold text-navy shadow-sm transition-all duration-300 hover:bg-slate-50 hover:-translate-y-0.5`) "Find a Job" + ArrowRight.
- Remember the JSX escaping rule: write H&amp;S (ampersand is actually fine raw too, but never a raw apostrophe in JSX text — the About/CTA copy above has none, keep it that way).
